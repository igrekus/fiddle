"""
#task
Написать функцию возвращающую строку, содержимое которой представляет собой вывод классического параметризованного FizzBuzz. Сигнатура:

```
def fizz_buzz(fizz: int, buzz: int, seq: Iterable) -> str:
    ...
```

где

`fizz`, `buzz` - любое натуральное число
`seq` - любая последовательность натуральных чисел

Пример:

```
>>> fizz_buzz(fizz=3, buzz=5, seq=range(1, 16))
1
2
Fizz
4
Buzz
Fizz
7
8
Fizz
Buzz
11
Fizz
13
14
FizzBuzz
```

Данные на входе будут всегда корректны:
    - делители всегда меньше максимального числа из последовательрности
    - делитель для fizz всегда меньше делителя для buzz как минимум на единицу
    - не выпендривайся, юзай `range()`

Задача со звёздочкой:

Решить задачу за два значимых действия. Значимые действия это
 - проверки на делимость (в том числе or и запрос в кеш)
 - вычисление ключа для записи в кеш для одного из делителей
 - сборка строки в переменной-аккумуляторе, в том числе зайинлайненной

Бойлерплейт в виде циклов, определения функций и лямбд, записи в кеш вычисленных ключей, вызов map/itertools/functools значимым действием не является.

Решения присылать в виде Python-модуля в личку игреку.
"""
